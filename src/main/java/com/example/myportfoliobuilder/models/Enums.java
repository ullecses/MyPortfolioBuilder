package com.example.myportfoliobuilder.models;

public class Enums {

    public enum BusinessTrips {
        NOT_SPECIFIED, POSSIBLE, IMPOSSIBLE, RARELY_POSSIBLE
    }

    public enum EmploymentType {
        FULL_TIME, PART_TIME, FREELANCE, INTERNSHIP
    }

    public enum WorkCharacter {
        ON_SITE, MOBILE, REMOTE
    }

    public enum WorkSchedule {
        FIXED, FULL_DAY, SHIFT, FLEXIBLE, ROTATIONAL
    }

    public enum Gender {
        MALE, FEMALE
    }

    public enum Role {
        USER, GUEST, ADMINISTRATOR
    }
}

